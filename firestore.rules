rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only authenticated users (Cameron & Emma via Google Sign-In) can access
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
